/*import React from 'react'
import { useSelector } from 'react-redux';
import { useDispatch } from 'react-redux';
import { increment, decrement } from '../State/actions';
export default function Trial() {
    const count = useSelector((state) => state);
    const dispatch = useDispatch();
    //onClick={() => dispatch(increment())}
  return (
    <div>
         <p>Count: {count}</p>
         <button onClick={() => dispatch(increment())}>Add 1</button>
        <button onClick={() => dispatch(decrement())}>Subtract 1</button>
    </div>
    )
}
//
1=>  add env file //done
2=>  use local storage //done
3=>  details before payment
4=>  create payment and waiting
5=>  add alerts
6=>  mini alerts
7=>  show order add cancel items
8=>  show carts 
9=>  search 
10=> filters
11=> responsive and color
12=> hosting

================next step===============
add google login , foragate password and otp validation


*/
